<!DOCTYPE html>
<html ng-app="app">
  <head>
    <meta charset="utf-8">
    <title>Trabajando con los Objetos - Practica Uno</title>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.4.8/angular.min.js"></script>
  </head>
  <body ng-controller="coleccion">
    <p>
      Aqui estoy utilizando la directiva ng-options con array simple
    </p>
    <div class="">
      <span>Articulos Deportivos:</span>
      <select class="" ng-model="articuloDeportivoOptions" ng-options="x for x in articulosArray track by x[0]">
      </select>
    </div>
    <p>
      Aqui estoy utilizando la directiva ng-repeat con array simple
    </p>
    <div class="">
      <span>Articulos Deportivos:</span>
      <select ng-model="articuloDeportivoRepeat" class="" name="">
        <option ng-repeat="x in articulosArray">{{x}}</option>
      </select>
    </div>
    <div class="">
      <p>
        Este es el articulo seleccionado por Options {{articuloDeportivoOptions}}
      </p>
      <p>
        Este es el articulo seleccionado por Repeat {{articuloDeportivoRepeat}}
      </p>
    </div>
    <hr>

    <p>
      Aqui estoy utilizando la directiva ng-options con array de objetos
    </p>
    <div class="">
      <span>Articulos Deportivos:</span>
      <select class="" ng-model="selectedOption" ng-options="y.nombre for y in persons.ArrayObjeto track by y.nombre">
      </select>
    </div>
    <p>
      Aqui estoy utilizando la directiva ng-repeat con array de objetos
    </p>
    <div class="">
      <span>Articulos Deportivos:</span>
      <select ng-model="selectUser2">
        <option ng-repeat="z in persons.ArrayObjeto" ng-selected="" value="{{z}}">{{z.nombre}}</option>
      </select>
    </div>
    <div class="">
      <p>
        Este es el usuario seleccionado por Options: {{persons.selectedOption}}
      </p>
      <p>
        Este es el usuario seleccionado por Repeat {{selectUser2}}
      </p>
    </div>
    <hr>

    <p>
      Aqui estoy utilizando la directiva ng-options con un objeto
    </p>
    <div class="">
      <span>Articulos Deportivos:</span>
      <select class="" ng-model="cintas.selected" ng-options="x for (x,y) in cintas.objeto track by y.id">
      </select>
    </div>

    <div class="">
      <p>
        Este es el usuario seleccionado por Options: {{cintas.selected}}
      </p>
    </div>
    <script type="text/javascript">
    alert (Math.random ());

    // Se cre solo una instancia que se llama MIAPLICACION
    // Creacion de un namespace global
    var  MIAPLICACION = MIAPLICACION | {};
    //sub-namespace
    // Creación del contenedor llamado MIAPLICACION.metodoComun de método y propiedades comunes.
    MIAPLICACION.metodoComun = {
      regExParaNombre: "", // define regex para la validación del nombre
      regExParaTelefono: "", // define regex para validación del teléfono
      validaNombre: function(nombre){
        // Hace algo con el nombre que usted ingresa a la variable reExParaNombre
        // usando "this.regExParaNombre"
      },

      validaNroTelefono: function (numTelefono){
        // Hace algo con el número de teléfono
      }
    }

    // Objeto junto a la declaración del método
    MIAPLICACION.event = {
      addListener: function(el, type, fn){
        // código de relleno
      },
      removeListener: function(el, type, fn){
        // código de relleno
      },
      getEvent: function(e) {
        // código de relleno
      }

      // Puedes agregar otras propiedades y métodos
    }

    // Sintaxis de utilización del método addListener:
    //MIAPLICACION.event.addListener("turel", "tipo", callback);
    </script>

    <script type="text/javascript">
      // Definimos el contructor persona
      function Persona(primerNombre) {
        this.primerNombre = primerNombre;
        this.caminar = function () {
          alert("estoy caminando");
        }
      };
      Persona.prototype.diHola = function () {
        alert("Hola, Soy" + this.primerNombre + ".Esta es la asignatura " + this.asignatura);
      };
      function Estudiante(primerNombre, asignatura) {
        Persona.call(this, primerNombre); // call super constructor.
        this.asignatura = asignatura;
      };
      // subclass extends superclass
      Estudiante.prototype = Object.create(Persona.prototype);
      Estudiante.prototype.constructor = Estudiante;

      var estudiante1 = new Estudiante("Carolina", "Física Aplicada");
      estudiante1.diHola();    // muestra "Hola, Soy Carolina. Estoy estudianto Física Aplicada."
      estudiante1.caminar();   // muestra "Yo estoy caminando !"

    </script>

    <script type="text/javascript">
      var app = angular.module('app',[]);
      app.controller('coleccion',['$scope', function (scope) {
          scope.articulosArray = ["Bicicvara","Patineta","Monopatin"];
          scope.persons = {
            ArrayObjeto:[
            {id: 1, nombre: "Samil",      apellido: "Sanchez"},
            {id: 2, nombre: "Elizabeth",  apellido: "Pinto"},
            {id: 3, nombre: "Norelis",    apellido: "Sanchez"}
            ]//,
            //selectedOption: {id: 1, name: "Norelis", apellido: "Sanchez"}
          };
          scope.selectedOption = {id: 1, nombre: "Norelis", apellido: "Sanchez"}
          scope.cintas = {
            objeto: {
              cinta001 : { id: 1 , color: 'white', metros: 100, gastado: 20 },
              cinta002 : { id: 2 , color: 'red', metros: 500, gastado: 200 },
              cinta003 : { id: 3 , color: 'blue', metros: 600, gastado: 260 },
              cinta004 : { id: 4 , color: 'black', metros: 10, gastado: 2 },
              cinta005 : { id: 5 , color: 'purple', metros: 809, gastado: 9 },
              cinta006 : { id: 6 , color: 'brown', metros: 100, gastado: 0 }
            },
            selected: {id: 2 , color: 'red', metros: 500, gastado: 200 }
          };
        scope.articuloDeportivoOptions = ["Bicicvara"];
      }]);
    </script>
  </body>
</html>
